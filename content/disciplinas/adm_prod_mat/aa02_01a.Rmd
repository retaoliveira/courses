---
date: "2023-03-235T00:00:00+01:00"
draft: false
menu:
  adm_prod_mat:
    parent: Atividades de aprendizagem
    weight: 20
title: 2. Previsão e informação de vendas - GRUPO 01
editor_options: 
  markdown: 
    wrap: 70
output: 
  blogdown::html_page:
    toc: true
type: docs
weight: 20
---



## 🔍 Análise Detalhada por Cenário

### **Cenário 1: Sazonalidade Clara (Componente Automotivo)**

```r
# DADOS E ESTATÍSTICAS
Demanda_Real = c(85, 92, 110, 125, 140, 165, 155, 130, 95, 80, 75, 70)
Média: 110.8 unidades | Desvio Padrão: 36.0 | CV: 32.5%
Tendência: -1.27 unidades/mês (decrescente suave)
```

**Padrão Identificado:** 
- **Sazonalidade forte** com pico no verão (meses 5-8: 125-165 unidades)
- Vale no inverno (meses 10-12: 70-80 unidades) 
- Comportamento previsível e repetitivo

**Análise Técnica:**
- Ideal para métodos de **decomposição clássica**
- Sazonalidade explica ≈60% da variação
- Previsões devem considerar o ciclo anual

---

### **Cenário 2: Crescimento Exponencial (Baterias VE)**
```r
# DADOS E ESTATÍSTICAS  
Demanda_Real = c(45, 52, 58, 65, 75, 88, 102, 120, 140, 165, 195, 230)
Média: 111.5 unidades | Desvio Padrão: 61.1 | CV: 54.8%
Tendência: +16.82 unidades/mês (crescimento forte)
R² da tendência: 0.98 (excelente ajuste)
```

**Padrão Identificado:**
- **Crescimento acelerado** constante
- Aumento de 411% em 12 meses (45 → 230 unidades)
- Mercado em expansão exponencial

**Análise Técnica:**
- Métodos devem **capturar tendência** acima de tudo
- Dados históricos totalmente relevantes
- Previsões requerem projeção de crescimento

---

### **Cenário 3: Alta Volatilidade (Compressor Industrial)**
```r
# DADOS E ESTATÍSTICAS
Demanda_Real = c(120, 85, 140, 75, 160, 95, 130, 110, 170, 80, 150, 100)
Média: 118.3 unidades | Desvio Padrão: 35.2 | CV: 29.8%
Tendência: +1.36 unidades/mês (quase estável)
```

**Padrão Identificado:**
- **Alta volatilidade** sem padrão claro
- Flutuações entre 75-170 unidades (variação de 127%)
- Comportamento imprevisível de clientes

**Análise Técnica:**
- Métodos complexos têm **pouca vantagem** sobre simples
- Foco em **flexibilidade operacional**
- Estoque de segurança mais importante que previsão precisa

## 💻 Códigos Principais Implementados

### **1. Função de Análise de Padrões**
```{r}
analisar_padrao_demanda <- function(dados) {
  modelo <- lm(Demanda_Real ~ Mes, data = dados)
  tendencia <- coefficients(modelo)[2]
  r2 <- summary(modelo)$r.squared
  cv <- sd(dados$Demanda_Real) / mean(dados$Demanda_Real) * 100
  
  tipo_padrao <- ifelse(cv > 40, "Alta Volatilidade",
                       ifelse(r2 > 0.8, "Tendência Forte", 
                             ifelse(abs(tendencia) > 10, "Crescimento", "Sazonal")))
  
  return(list(tendencia = tendencia, r2 = r2, cv = cv, tipo = tipo_padrao))
}
```

### **2. Funções de Previsão por Cenário**
```{r}
# CENÁRIO 1 - SAZONALIDADE
prever_sazonal <- function(dados) {
  # Decomposição sazonal simples
  media_verao <- mean(dados$Demanda_Real[5:8])  # Meses 5-8
  media_inverno <- mean(dados$Demanda_Real[10:12]) # Meses 10-12
  return(list(
    previsao_mes13 = media_inverno,  # Mês 13 é inverno
    tecnica = "Média Sazonal",
    intervalo = c(media_inverno*0.9, media_inverno*1.1)
  ))
}

# CENÁRIO 2 - CRESCIMENTO  
prever_crescimento <- function(dados) {
  modelo <- lm(Demanda_Real ~ Mes, data = dados)
  previsao <- predict(modelo, newdata = data.frame(Mes = 13))
  return(list(
    previsao_mes13 = round(previsao, 1),
    tecnica = "Regressão Linear",
    r2 = summary(modelo)$r.squared
  ))
}

# CENÁRIO 3 - VOLATILIDADE
prever_volatil <- function(dados) {
  mms <- mean(tail(dados$Demanda_Real, 3))  # MMS n=3
  return(list(
    previsao_mes13 = mms,
    tecnica = "Média Móvel Simples (n=3)",
    estoque_seguranca = sd(dados$Demanda_Real) * 1.5
  ))
}
```

### **3. Geração de Relatórios Automatizados**
```{r}
gerar_relatorio_cenario <- function(cenario, numero) {
  analise <- analisar_padrao_demanda(cenario$dados)
  
  cat("=== CENÁRIO", numero, "===\n")
  cat("Produto:", cenario$produto, "\n")
  cat("Padrão:", analise$tipo, "\n")
  cat("CV:", round(analise$cv, 1), "% | Tendência:", round(analise$tendencia, 2), "/mês\n")
  
  # Aplicar técnica recomendada
  if(analise$tipo == "Sazonal") {
    resultado <- prever_sazonal(cenario$dados)
  } else if(analise$tipo == "Crescimento") {
    resultado <- prever_crescimento(cenario$dados)  
  } else {
    resultado <- prever_volatil(cenario$dados)
  }
  
  cat("Técnica:", resultado$tecnica, "\n")
  cat("Previsão Mês 13:", resultado$previsao_mes13, "unidades\n")
}
```


## 📈 Resultados Esperados por Cenário

### **Cenário 1 - Sazonalidade Clara**
```
PREVISÃO MÊS 13: ≈70-80 unidades (período de inverno)
TÉCNICA: Média Sazonal ou Decomposição
MAPE ESPERADO: 8-12% (alta precisão devido ao padrão claro)
AÇÕES: Reduzir produção, programar manutenção, manter estoque mínimo
```

### **Cenário 2 - Crescimento Exponencial**  
```
PREVISÃO MÊS 13: ≈250-260 unidades (projeção linear)
TÉCNICA: Regressão Linear
MAPE ESPERADO: 5-8% (excelente ajuste)
AÇÕES: Expandir capacidade, contratar equipe, aumentar supply chain
```

### **Cenário 3 - Alta Volatilidade**
```
PREVISÃO MÊS 13: ≈110-130 unidades (próximo à média)
TÉCNICA: MMS + Estoque de Segurança
MAPE ESPERADO: 15-25% (baixa precisão inerente)
AÇÕES: Manter flexibilidade, estoque de segurança, lead times curtos
```

## 🎯 Respostas Esperadas dos Grupos

### **Análise Qualitativa Esperada:**
- **Cenário 1:** Identificar sazonalidade e planejar capacidade diferenciada
- **Cenário 2:** Reconhecer crescimento de mercado e necessidade de expansão  
- **Cenário 3:** Aceitar imprevisibilidade e focar em resiliência operacional

### **Escolha de Método Esperada:**
- **Grupo 1 (Sazonal):** Deve escolher método sazonal, não MMS
- **Grupo 2 (Crescimento):** Deve priorizar métodos com tendência
- **Grupo 3 (Volátil):** Deve optar por simplicidade + estoque segurança


## 📊 Métricas de Avaliação dos Grupos

### **Critérios Quantitativos (60% da nota):**
- **Cálculos corretos:** 25 pontos
- **MAPE calculado adequadamente:** 15 pontos  
- **Previsão Mês 13 dentro do esperado:** 20 pontos

### **Critérios Qualitativos (40% da nota):**
- **Análise do padrão:** 10 pontos
- **Escolha do método justificada:** 10 pontos
- **Recomendações práticas adequadas:** 10 pontos
- **Integração quali-quanti:** 10 pontos

## ✅ Conclusão da Síntese

Os **3 cenários** representam desafios **distintos e realistas** de previsão:
1. **Padrão previsível** (Sazonalidade) → Foco em métodos clássicos
2. **Tendência clara** (Crescimento) → Foco em projeção  
3. **Imprevisibilidade** (Volatilidade) → Foco em resiliência

Cada cenário testa habilidades diferentes dos alunos, desde **análise técnica** até **tomada de decisão operacional**, preparando-os para desafios reais de administração da produção.



### Dados de Demanda Real:

```{r message=FALSE, warning=FALSE, include=FALSE}
demanda_sazonal <- data.frame(Mes = 1:12,
                              Demanda_Real = c(85, 92, 110, 125, 140, 165, 155, 130, 95, 80, 75, 70))
```
 

### Informações Qualitativas:

**Produto**: Componente para sistema de ar-condicionado automotivo

**Sazonalidade**: Demanda alta no verão (meses 5-8), baixa no inverno

**Fatores Externos:**
- Ondas de calor recorde previstas para próximo verão   
- Campanha governamental de inspeção veicular no trimestre 3   
- Concorrente principal com recall de produto

### Objetivos do planejamento

* "QUESTÃO-CHAVE: Como lidar com a forte sazonalidade?",
* "DESAFIO: Planejar produção para pico de verão sem criar excesso de estoque no inverno",
* "FOCOS: Gestão de capacidade sazonal, contratação temporária, programação de manutenção"
    
```{r message=FALSE, warning=FALSE, include=FALSE}
# =============================================================================
# TRÊS NOVOS CENÁRIOS PARA ATIVIDADE EM GRUPO
# =============================================================================

library(ggplot2)
library(dplyr)
library(kableExtra)

# -----------------------------------------------------------------------------
# CENÁRIO 1: DEMANDA SAZONAL FORTE (Componente Automotivo)
# -----------------------------------------------------------------------------
cenario_sazonal <- list(
  nome = "Cenário 1 - Componente Automotivo (Sazonal)",
  dados = data.frame(
    Mes = 1:12,
    Demanda_Real = c(85, 92, 110, 125, 140, 165, 155, 130, 95, 80, 75, 70)
  ),
  info_qualitativas = c(
    "Produto: Componente para sistema de ar-condicionado automotivo",
    "Sazonalidade: Demanda alta no verão (meses 5-8), baixa no inverno",
    "Fatores: Ondas de calor recorde previstas para próximo verão",
    "Campanha governamental de inspeção veicular no trimestre 3",
    "Concorrente principal com recall de produto"
  ),
  produto = "Componente Automotivo",
  unidade = "unidades/mês"
)

gerar_relatorio_cenario(cenario_sazonal)



```
```{r}
gerar_relatorio_completo <- function(cenario, numero) {
  
  cat("\n", rep("=", 80), "\n", sep = "")
  cat("📊 ", numero, ". ", cenario$nome, "\n", sep = "")
  cat(rep("=", 80), "\n\n")
  
  # Análise avançada
  analise <- analisar_padrao_avancado(cenario$dados)
  
  # Tabela de dados com destaque para mudanças
  tabela_dados <- cenario$dados
  if (!is.na(analise$ponto_mudanca)) {
    tabela_dados <- tabela_dados %>%
      mutate(Destaque = ifelse(Mes >= analise$ponto_mudanca, "🔴 Pós-Mudança", "🔵 Padrão Antigo"))
  } else {
    tabela_dados$Destaque <- "🔵 Normal"
  }
  
  print(
    tabela_dados %>%
      kable(format = "html", caption = paste("Dados Históricos -", cenario$desafio)) %>%
      kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) %>%
      row_spec(0, bold = TRUE, background = "#f8f9fa") %>%
      {if(!is.na(analise$ponto_mudanca)) 
        row_spec(., analise$ponto_mudanca:nrow(tabela_dados), bold = TRUE, background = "#ffe6e6") 
       else .}
  )
  
  # Informações qualitativas
  cat("\n<strong>🎯 DESAFIO PRINCIPAL:</strong> ", cenario$desafio, "\n")
  cat("\n<strong>📝 CONTEXTO QUALITATIVO:</strong>\n")
  cat("<ul>")
  for (info in cenario$info_qualitativas) {
    cat("<li>", info, "</li>")
  }
  cat("</ul>\n")
  
  # Gráfico com análise
  p <- ggplot(cenario$dados, aes(x = Mes, y = Demanda_Real)) +
    geom_line(linewidth = 1.2, color = "#1f77b4") +
    geom_point(size = 3, color = "#1f77b4") +
    {if(!is.na(analise$ponto_mudanca)) 
      geom_vline(xintercept = analise$ponto_mudanca - 0.5, 
                 linetype = "dashed", color = "red", linewidth = 1)
    } +
    labs(
      title = paste("Padrão de Demanda -", cenario$produto),
      subtitle = paste("Classificação:", analise$tipo_padrao),
      x = "Mês",
      y = paste("Demanda (", cenario$unidade, ")")
    ) +
    theme_minimal() +
    theme(
      plot.title = element_text(face = "bold", size = 14, hjust = 0.5),
      plot.subtitle = element_text(size = 10, hjust = 0.5, color = "red")
    ) +
    scale_x_continuous(breaks = 1:12)
  
  print(p)
  
  # Estatísticas detalhadas
  cat("\n<strong>📈 METRÍCAS AVANÇADAS:</strong>\n")
  cat("<ul>")
  cat("<li><strong>Padrão Identificado:</strong> ", analise$tipo_padrao, "</li>")
  cat("<li><strong>Tendência Mensal:</strong> ", round(analise$tendencia, 2), " unidades/mês</li>")
  cat("<li><strong>R² da Tendência:</strong> ", round(analise$r2_tendencia, 3), "</li>")
  cat("<li><strong>Força Sazonal:</strong> ", round(analise$forca_sazonal, 1), "</li>")
  cat("<li><strong>Coef. de Variação:</strong> ", round(analise$variabilidade, 1), "%</li>")
  if (!is.na(analise$ponto_mudanca)) {
    cat("<li><strong>Mudança Estrutural:</strong> Mês ", analise$ponto_mudanca, "</li>")
  }
  cat("<li><strong>Complexidade de Previsão:</strong> ", analise$complexidade, "</li>")
  cat("</ul>\n")
}

# =============================================================================
# APLICAR AOS 6 CENÁRIOS
# =============================================================================

# Lista completa de cenários
cenarios_completos <- list(
  cenario_sazonal,      # Já definido anteriormente
  cenario_crescimento,  # Já definido anteriormente  
  cenario_instavel,     # Já definido anteriormente
  cenario_quebra,
  cenario_sazonal_complexa,
  cenario_influencia
)

nomes_cenarios <- c(
  "1 - Sazonalidade Clara",
  "2 - Crescimento Exponencial", 
  "3 - Alta Volatilidade",
  "4 - Quebra de Padrão",
  "5 - Sazonalidade Complexa",
  "6 - Influência Externa"
)

# Gerar relatórios
for (i in 1:6) {
  gerar_relatorio_completo(cenarios_completos[[i]], nomes_cenarios[i])
}

# =============================================================================
# TABELA COMPARATIVA DOS 6 CENÁRIOS
# =============================================================================

tabela_comparativa_6 <- data.frame(
  Cenário = nomes_cenarios,
  Produto = c("Componente Automotivo", "Baterias VE", "Compressor Industrial",
              "Microcontrolador IoT", "Sensor Agrícola", "Inversor Solar"),
  Demanda_Média = sapply(cenarios_completos, function(x) mean(x$dados$Demanda_Real)),
  Variabilidade = sapply(cenarios_completos, function(x) 
    round(sd(x$dados$Demanda_Real) / mean(x$dados$Demanda_Real) * 100, 1)),
  Padrão = sapply(cenarios_completos, function(x) 
    analisar_padrao_avancado(x$dados)$tipo_padrao),
  Complexidade = sapply(cenarios_completos, function(x) 
    analisar_padrao_avancado(x$dados)$complexidade),
  Técnica_Recomendada = c(
    "Decomposição Sazonal", "Regressão Linear", "MMS + Estoque Segurança",
    "Dados Recentes Only", "ARIMA Sazonal", "Modelo Causal"
  )
)

print(
  tabela_comparativa_6 %>%
    mutate(Demanda_Média = round(Demanda_Média, 1)) %>%
    kable(format = "html", caption = "Comparação dos Seis Cenários de Demanda") %>%
    kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) %>%
    row_spec(0, bold = TRUE, background = "#2c3e50", color = "white") %>%
    column_spec(1, bold = TRUE) %>%
    column_spec(6, color = "#c0392b", bold = TRUE)
)

# =============================================================================
# GUIA DE ORIENTAÇÃO PARA OS GRUPOS
# =============================================================================

cat("\n", rep("=", 80), "\n", sep = "")
cat("🎯 GUIA DE ORIENTAÇÃO POR TIPO DE CENÁRIO\n")
cat(rep("=", 80), "\n\n")

orientacoes <- list(
  "Sazonalidade Clara" = c(
    "• Focar em identificar período e amplitude da sazonalidade",
    "• Usar decomposição clássica ou médias móveis sazonais", 
    "• Planejar capacidade para picos e vales específicos"
  ),
  
  "Crescimento Exponencial" = c(
    "• Priorizar métodos que capturam tendência (regressão, SES α alto)",
    "• Considerar transformação logarítmica dos dados",
    "• Planejar expansão gradual de capacidade"
  ),
  
  "Alta Volatilidade" = c(
    "• Usar métodos simples (MMS) com estoque de segurança",
    "• Focar em flexibilidade operacional vs. previsão perfeita",
    "• Estabelecer buffers de capacidade e matéria-prima"
  ),
  
  "Quebra de Padrão" = c(
    "• Descartar dados antigos, usar apenas informações recentes",
    "• Adotar previsões de curto prazo com revisão frequente",
    "• Desenvolver cenários alternativos (otimista/pessimista)"
  ),
  
  "Sazonalidade Complexa" = c(
    "• Utilizar modelos ARIMA sazonais ou redes neurais",
    "• Incorporar variáveis externas (clima, calendário agrícola)",
    "• Trabalhar com previsões por região/segmento"
  ),
  
  "Influência Externa" = c(
    "• Desenvolver modelos causais com variáveis macroeconômicas",
    "• Implementar sistema de alerta para eventos externos",
    "• Mancer estoque estratégico para absorver volatilidade"
  )
)

for (i in 1:6) {
  cat("\n<strong>", nomes_cenarios[i], ":</strong>\n")
  cat("<ul>")
  for (item in orientacoes[[i]]) {
    cat("<li>", item, "</li>")
  }
  cat("</ul>")
}

# =============================================================================
# SALVAR TODOS OS CENÁRIOS
# =============================================================================

# Salvar dados em arquivos CSV
for (i in 1:6) {
  arquivo <- paste0("cenario_", i, "_", 
                   gsub(" ", "_", tolower(cenarios_completos[[i]]$produto)), ".csv")
  write.csv(cenarios_completos[[i]]$dados, arquivo, row.names = FALSE)
}

# Criar arquivo de metadados
metadados <- data.frame(
  Cenário = 1:6,
  Nome = sapply(cenarios_completos, function(x) x$nome),
  Produto = sapply(cenarios_completos, function(x) x$produto),
  Desafio = sapply(cenarios_completos, function(x) x$desafio),
  Arquivo = paste0("cenario_", 1:6, "_", 
                  sapply(cenarios_completos, function(x) 
                    gsub(" ", "_", tolower(x$produto))), ".csv")
)

write.csv(metadados, "metadados_cenarios.csv", row.names = FALSE)

cat("\n✅ <strong>Seis cenários criados com sucesso!</strong>\n")
cat("📁 <strong>Arquivos gerados:</strong>\n")
for (i in 1:6) {
  cat("   - cenario_", i, "_", 
      gsub(" ", "_", tolower(cenarios_completos[[i]]$produto)), ".csv\n", sep = "")
}
cat("   - metadados_cenarios.csv\n\n")

cat("🎯 <strong>Distribuição recomendada para 6 grupos:</strong>\n")
for (i in 1:6) {
  cat("   Grupo ", i, ": ", nomes_cenarios[i], "\n", sep = "")
}
```

